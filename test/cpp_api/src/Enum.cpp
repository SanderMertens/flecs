#include <cpp_api.h>

enum Color {
    Red,
    Green,
    Blue
};

void Enum_add_enum_constant() {
    // flecs::world ecs;

    // auto e = ecs.entity().add(Red);
    // test_assert(e.has(Red));
    // test_assert(e.has<Color>(Red));

    // test_int(e.type().count(), 1);

    // flecs::id id = e.type().get(0);
    // test_bool(id.is_pair(), true);
    // test_assert(id.relation() == ecs.id<Color>());
    // // test_assert(id.object() == ecs.id<Red>());
}

void Enum_add_2_enum_constants() {
    // Implement testcase
}
